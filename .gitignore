# Credentials / secrets — API keys are read from environment variables only, never committed.
.env
.env.*
*.env
secrets.toml
.streamlit/secrets.toml
*.pem
*.key

# Python build artefacts
__pycache__/
*.pyc
*.pyo

# Transient: created and removed by run_all.py --verify on every run
decision_engine/_locked_snapshot/

# NOTE: .baseline/ is NOT ignored - validate_phase4_nearby.py reads it as a required
# input (md5 hashes proving protected files are unchanged). It contains hashes only.
